<div class="map-card">
    <%= link_to experience_path(experience) do %>
      <%= image_tag experience.image_url, height: 100, width: 200, crop: :fill if experience.image_url %>
    <div>
      <h1><%= experience.name %></h1>
      <%= experience.address %>
    </div>
      <% end %>
    </div>
</div>
